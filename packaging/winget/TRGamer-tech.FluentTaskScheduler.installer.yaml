# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.12.0.schema.json
# ---------------------------------------------------------------------------
# NOTE TO MAINTAINER: Before submitting a PR to winget-pkgs, replace every
# <SHA256_OF_...> placeholder with the real SHA-256 hash of the corresponding
# file from the GitHub Release. You can compute hashes with:
#   (Get-FileHash "Setup-x64.msi" -Algorithm SHA256).Hash
# ---------------------------------------------------------------------------

PackageIdentifier: TRGamer-tech.FluentTaskScheduler
PackageVersion: 1.9.0
MinimumOSVersion: "10.0.17763.0"
InstallerType: wix
UpgradeBehavior: install
# ProductCode is set per-installer below, not at the root: WiX/VeloPack regenerates a fresh
# ProductCode on every `vpk pack` run, and x64/arm64 builds get distinct codes even within the
# same version. Verify with:
#   (New-Object -ComObject WindowsInstaller.Installer).OpenDatabase($msiPath, 0) ... query the
#   Property table for 'ProductCode' (see CONTRIBUTING.md), or `msiexec /a Setup-x64.msi` prompts.
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/TRGamer-tech/FluentTaskScheduler/releases/download/V1.9.0/Setup-x64.msi
    InstallerSha256: C21DBD04F6C50517370DE0DC6F604A921A932DE9CD75DA7FEC717E4ADF4F7A6C
    ProductCode: "{CC749647-F531-4715-BC20-BB448E1A9B41}"
  - Architecture: arm64
    InstallerUrl: https://github.com/TRGamer-tech/FluentTaskScheduler/releases/download/V1.9.0/Setup-arm64.msi
    InstallerSha256: 4CB3BD07E20FC8990A31F7D89BCEBBD8EE2ED8B77BCEE52C6345DFD3FAFF28D6
    ProductCode: "{9C27F4BB-967B-41DC-88A1-3437231854F1}"
ManifestType: installer
ManifestVersion: 1.12.0
