<UserControl
    x:Class="FluentTaskScheduler.Dialogs.HistoryEntryDetailDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentTaskScheduler.Dialogs"
    xmlns:models="using:FluentTaskScheduler.Models">

    <Grid MinWidth="750" MaxWidth="900" Margin="0,10,0,0">
        <StackPanel Spacing="16">
            <!-- Header Info -->
            <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8">
                <StackPanel Spacing="4">
                    <TextBlock Text="{x:Bind Entry.Result}" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource SystemControlBackgroundAccentBrush}"/>
                    <TextBlock Text="{x:Bind Entry.Time}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                </StackPanel>
            </Grid>

            <!-- Detailed Info Grid -->
            <Grid ColumnSpacing="20" RowSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                    <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Event ID:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind Entry.EventId}"/>

                <TextBlock Grid.Row="1" Text="Exit Code:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind Entry.ExitCode}" IsTextSelectionEnabled="True"/>

                <TextBlock Grid.Row="2" Text="Level:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind Entry.Level}"/>

                <TextBlock Grid.Row="3" Text="User:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{x:Bind Entry.User}" IsTextSelectionEnabled="True"/>

                <TextBlock Grid.Row="4" Text="OpCode:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{x:Bind Entry.OpCode}"/>

                <TextBlock Grid.Row="5" Text="Task Category:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{x:Bind Entry.TaskCategory}"/>

                <TextBlock Grid.Row="6" Text="Keywords:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{x:Bind Entry.Keywords}"/>

                <TextBlock Grid.Row="7" Text="Computer:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="7" Grid.Column="1" Text="{x:Bind Entry.Computer}"/>

                <TextBlock Grid.Row="8" Text="Correlation ID:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Grid.Row="8" Grid.Column="1" Text="{x:Bind Entry.ActivityId}" FontSize="11" Opacity="0.8" IsTextSelectionEnabled="True"/>
            </Grid>

            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,8"/>

            <!-- Log Message -->
            <StackPanel Spacing="8">
                <TextBlock Text="General Message:" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        CornerRadius="8" Padding="12">
                    <TextBlock Text="{x:Bind Entry.Message}" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
                </Border>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12" Margin="0,16,0,0">
                <Button Content="Copy Details" Click="CopyButton_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="Close" Click="CloseButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
