<Page
    x:Class="FluentTaskScheduler.MainPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentTaskScheduler"
    xmlns:models="using:FluentTaskScheduler.Models"
    xmlns:converters="using:FluentTaskScheduler.Converters"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    Loaded="MainPage_Loaded"
    SizeChanged="Page_SizeChanged"
    Background="Transparent">

    <Page.KeyboardAccelerators>

        <KeyboardAccelerator Key="N" Modifiers="Control" Invoked="NewTaskAccelerator_Invoked"/>
        <KeyboardAccelerator Key="E" Modifiers="Control" Invoked="EditTaskAccelerator_Invoked"/>
        <KeyboardAccelerator Key="R" Modifiers="Control" Invoked="RunTaskAccelerator_Invoked"/>
        <KeyboardAccelerator Key="Delete" Invoked="DeleteTaskAccelerator_Invoked"/>
        <KeyboardAccelerator Key="Escape" Invoked="EscapeAccelerator_Invoked"/>
        <KeyboardAccelerator Key="F1" Invoked="ShortcutsAccelerator_Invoked"/>
    </Page.KeyboardAccelerators>

    <Page.Resources>
        <converters:StateToIconConverter x:Key="StateToIconConverter"/>
        <converters:StateToColorConverter x:Key="StateToColorConverter"/>
        <converters:DateTimeConverter x:Key="DateTimeConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Page.Resources>

    <Grid>
        <!-- Custom Title Bar Drag Region -->
        <Grid x:Name="AppTitleBar" Height="32" VerticalAlignment="Top" Canvas.ZIndex="100">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48"/> <!-- PaneToggle button area -->
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/> <!-- Caption buttons area -->
            </Grid.ColumnDefinitions>
            <!-- The middle part is draggable -->
            <Border Grid.Column="1" Background="Transparent" x:Name="AppTitleBarDragArea"/>
        </Grid>

        <NavigationView x:Name="NavView"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        SelectionChanged="NavView_SelectionChanged"
                        ItemInvoked="NavView_ItemInvoked"
                        Background="Transparent">

            <NavigationView.PaneCustomContent>
                <StackPanel>
                    <StackPanel x:Name="FolderSection" Visibility="{x:Bind NavView.IsPaneOpen, Mode=OneWay}">
                        <Grid Margin="10,5,10,4">
                            <TextBlock Text="Folders" 
                                      FontWeight="SemiBold" 
                                      VerticalAlignment="Center"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="4">
                                <Button x:Name="FolderRefreshBtn" Padding="4" Background="Transparent" BorderThickness="0" Click="ReloadFolders_Click" ToolTipService.ToolTip="Reload Folders">
                                    <Grid Width="14" Height="14">
                                        <Viewbox>
                                            <SymbolIcon x:Name="FolderRefreshIcon" Symbol="Refresh" />
                                        </Viewbox>
                                        <ProgressRing x:Name="FolderRefreshRing" IsActive="False" Visibility="Collapsed" />
                                    </Grid>
                                </Button>
                                <Button x:Name="FolderCreateBtn" Padding="4" Background="Transparent" BorderThickness="0" Click="CreateRootFolder_Click" ToolTipService.ToolTip="New Folder">
                                    <Viewbox Width="14" Height="14">
                                        <SymbolIcon Symbol="Add" />
                                    </Viewbox>
                                </Button>
                            </StackPanel>
                        </Grid>
                        <TreeView x:Name="FolderTreeView" 
                                  SelectionMode="Single"
                                  ItemInvoked="FolderTreeView_ItemInvoked"
                                  RightTapped="FolderTreeViewItem_RightTapped"
                                  AllowDrop="True"
                                  CanDragItems="False"
                                  CanReorderItems="False"
                                  DragOver="FolderTreeView_DragOver"
                                  DragLeave="FolderTreeView_DragLeave"
                                  Drop="FolderTreeView_Drop"
                                  Margin="4,0,0,8">
                            <TreeView.ItemContainerTransitions>
                                <TransitionCollection>
                                    <ContentThemeTransition/>
                                    <EntranceThemeTransition IsStaggeringEnabled="True"/>
                                </TransitionCollection>
                            </TreeView.ItemContainerTransitions>
                            <TreeView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="27" Padding="0" Background="Transparent"
                                          AllowDrop="True"
                                          CanDrag="False"
                                          DragOver="FolderItem_DragOver"
                                          DragLeave="FolderItem_DragLeave"
                                          Drop="FolderItem_Drop">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Grid.Column="0"
                                                 Glyph="&#xE8B7;" 
                                                 FontSize="16"
                                                 Margin="0,0,12,0"
                                                 VerticalAlignment="Center"
                                                 CanDrag="False"/>
                                        <TextBlock Grid.Column="1" 
                                                  Text="{Binding}"
                                                  VerticalAlignment="Center"
                                                  TextTrimming="CharacterEllipsis"
                                                  FontSize="14"
                                                  CanDrag="False"/>
                                        <!-- Drag handle: Moved to the right -->
                                        <FontIcon Grid.Column="2"
                                                  Glyph="&#xE76F;"
                                                  FontFamily="Segoe MDL2 Assets"
                                                  FontSize="12"
                                                  Opacity="0.6"
                                                  Margin="12,0,8,0"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  CanDrag="True"
                                                  DragStarting="FolderItem_DragStarting"
                                                  ToolTipService.ToolTip="Drag to move folder"/>
                                    </Grid>
                                </DataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </StackPanel>
                </StackPanel>
            </NavigationView.PaneCustomContent>

            <NavigationView.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Style="{StaticResource TitleTextBlockStyle}" Margin="-24,0,0,0" VerticalAlignment="Center"/>
                </DataTemplate>
            </NavigationView.HeaderTemplate>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="NavAdd" Tag="Add" Icon="Add" Content="New Task" SelectsOnInvoked="False" />
                <NavigationViewItem x:Name="NavAllTasks" Tag="all" Icon="List" Content="All Tasks"/>
                <NavigationViewItem x:Name="NavDashboard" Tag="Dashboard" Icon="ViewAll" Content="Dashboard" />
                <NavigationViewItem x:Name="NavQuickActions" Tag="QuickActions" Content="Quick Actions">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC4A;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="NavScriptLibrary" Tag="ScriptLibrary" Icon="Library" Content="Library" />
                <NavigationViewItem x:Name="NavScriptEditor" Tag="ScriptEditor" Icon="Edit" Content="Script Editor" />
                <NavigationViewItem x:Name="NavSettings" Tag="settings" Content="Settings"
                                   PointerEntered="Settings_PointerEntered"
                                   PointerExited="Settings_PointerExited">
                    <NavigationViewItem.Icon>
                        <AnimatedIcon x:Name="SettingsAnimatedIcon">
                            <AnimatedIcon.Source>
                                <animatedvisuals:AnimatedSettingsVisualSource/>
                            </AnimatedIcon.Source>
                            <AnimatedIcon.FallbackIconSource>
                                <SymbolIconSource Symbol="Setting"/>
                            </AnimatedIcon.FallbackIconSource>
                        </AnimatedIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Global Snooze banner (v1.9) -->
                <InfoBar x:Name="SnoozeBanner"
                         Grid.Row="0"
                         Severity="Warning"
                         IsOpen="False"
                         IsClosable="False"
                         Margin="16,8,16,0">
                    <InfoBar.ActionButton>
                        <Button x:Name="SnoozeBannerResumeBtn" Click="SnoozeResume_Click"/>
                    </InfoBar.ActionButton>
                </InfoBar>

                <Grid x:Name="TasksViewGrid" Grid.Row="1">
                     <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                
                <!-- Admin Drag & Drop Warning -->
                <InfoBar x:Name="AdminDragWarning"
                         Severity="Informational"
                         Title="Drag &amp; Drop Restricted"
                         Message="Windows does not support drag-and-drop operations when the app is running as Administrator."
                         IsClosable="True"
                         Margin="16,8,16,0"
                         Visibility="Collapsed"
                         Grid.Row="0"
                         Canvas.ZIndex="100"/>
                
                <!-- Loading Ring -->
                <ProgressRing x:Name="LoadingRing" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Canvas.ZIndex="100" Grid.RowSpan="2"/>
                
                <!-- Batch Action Bar (Appears when multiple items selected) -->
                <Grid x:Name="BatchActionBar" Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" 
                      VerticalAlignment="Top" Height="60" Margin="16,16,16,0" CornerRadius="8" Visibility="Collapsed" Canvas.ZIndex="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock x:Name="BatchCountText" Text="0 selected" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Rectangle Width="1" Height="24" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                        <Button x:Name="BatchRunBtn" Click="BatchRun_Click" ToolTipService.ToolTip="Run Selected">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Play"/>
                                <TextBlock Text="Run"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BatchStopBtn" Click="BatchStop_Click" ToolTipService.ToolTip="Stop Selected">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Stop"/>
                                <TextBlock Text="Stop"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="0,0,16,0">
                       <Button x:Name="BatchEnableBtn" Click="BatchEnable_Click" ToolTipService.ToolTip="Enable Selected">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Accept"/>
                                <TextBlock Text="Enable"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BatchDisableBtn" Click="BatchDisable_Click" ToolTipService.ToolTip="Disable Selected">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Cancel"/>
                                <TextBlock Text="Disable"/>
                            </StackPanel>
                        </Button>
                        <Rectangle Width="1" Height="24" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                        <Button x:Name="BatchDeleteBtn" Click="BatchDelete_Click" Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}" Foreground="White" ToolTipService.ToolTip="Delete Selected">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Delete"/>
                                <TextBlock Text="Delete"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BatchCancelBtn" Click="BatchCancel_Click" Background="Transparent" BorderThickness="0" ToolTipService.ToolTip="Clear Selection">
                            <SymbolIcon Symbol="Cancel"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="16,24,0,12" Spacing="8">

                    <AutoSuggestBox x:Name="SearchBox" QueryIcon="Find" PlaceholderText="Search tasks..." Width="260" TextChanged="SearchBox_TextChanged"/>

                    <!-- Status filter (replaces the former Running/Enabled/Disabled sidebar entries) -->
                    <ComboBox x:Name="StatusFilterBox" MinWidth="150" SelectedIndex="0" VerticalAlignment="Center"
                              SelectionChanged="StatusFilterBox_SelectionChanged">
                        <ComboBoxItem x:Name="StatusFilterAll" Tag="all" Content="All statuses"/>
                        <ComboBoxItem x:Name="StatusFilterRunning" Tag="running" Content="Running"/>
                        <ComboBoxItem x:Name="StatusFilterEnabled" Tag="enabled" Content="Enabled"/>
                        <ComboBoxItem x:Name="StatusFilterDisabled" Tag="disabled" Content="Disabled"/>
                        <ComboBoxItem x:Name="StatusFilterSnoozed" Tag="snoozed" Content="Snoozed"/>
                    </ComboBox>

                    <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" />
                    <Button x:Name="ImportTaskButton" Content="Import Task" Click="ImportTask_Click" />
                    <Button x:Name="SortButton" Content="Sort ↕" Click="SortButton_Click" />

                    <!-- Global snooze lives next to Sort so it sits with the other list-wide actions -->
                    <Button x:Name="SnoozeToolbarButton" Click="SnoozeToolbarButton_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon x:Name="SnoozeToolbarIcon" Glyph="&#xE769;" FontSize="14"/>
                            <TextBlock x:Name="SnoozeToolbarText" Text="Snooze All"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="ShortcutsButton" Content="?" Width="38" ToolTipService.ToolTip="Keyboard Shortcuts (F1)" Click="ShortcutsButton_Click" />
                </StackPanel>

                <ListView x:Name="TaskListView" 
                          Grid.Row="1"
                          SelectionMode="Extended"
                          IsItemClickEnabled="True"
                          ItemClick="TaskListView_ItemClick"
                          SelectionChanged="TaskListView_SelectionChanged"
                          ItemsSource="{x:Bind FilteredTasks}"
                          CanDragItems="True"
                          DragItemsStarting="TaskListView_DragItemsStarting"
                          Padding="0">
                    <!-- Disable default animations for snappy filtering -->
                    <ListView.ItemContainerTransitions>
                        <TransitionCollection/>
                    </ListView.ItemContainerTransitions>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ScheduledTaskModel">
                            <Grid Padding="12" Margin="0,4" CornerRadius="8" Background="{ThemeResource TaskCardBackground}" BorderThickness="1" BorderBrush="{ThemeResource TaskCardBorder}"
                                  Opacity="{x:Bind IsHidden, Mode=OneWay, Converter={StaticResource BoolToOpacityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,16,0">
                                    <CheckBox IsChecked="{x:Bind IsSelected, Mode=OneWay}" MinWidth="0" Margin="0,0,4,0" Click="TaskCheckBox_Click" VerticalAlignment="Center"/>
                                    <!-- Status icon: animated ring when IsRunning, static icon otherwise -->
                                    <Grid Width="24" Height="24" VerticalAlignment="Center">
                                        <ProgressRing IsActive="True"
                                                      Width="22" Height="22"
                                                      Visibility="{x:Bind IsRunning, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                      Foreground="{x:Bind State, Mode=OneWay, Converter={StaticResource StateToColorConverter}}"/>
                                        <FontIcon Glyph="{x:Bind State, Mode=OneWay, Converter={StaticResource StateToIconConverter}}"
                                                  Foreground="{x:Bind State, Mode=OneWay, Converter={StaticResource StateToColorConverter}}"
                                                  FontSize="20"
                                                  Visibility="{x:Bind IsRunning, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    </Grid>
                                </StackPanel>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}" 
                                                CornerRadius="4" Padding="4,0" 
                                                Visibility="{x:Bind Category, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}"
                                                Tapped="CategoryBadge_Tapped" ToolTipService.ToolTip="Filter by this category"
                                                MaxWidth="150">
                                            <TextBlock Text="{x:Bind Category}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="White" FontSize="10" TextTrimming="CharacterEllipsis"/>
                                        </Border>
                                        <Border Background="{ThemeResource SystemFillColorNeutralBrush}" 
                                                CornerRadius="4" Padding="4,0" 
                                                Visibility="{x:Bind IsHidden, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                                            <TextBlock Text="Hidden" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="10"/>
                                        </Border>
                                        <Border Background="{ThemeResource SystemFillColorNeutralBrush}" 
                                                CornerRadius="4" Padding="4,0" 
                                                Visibility="{x:Bind IsFromEventLog, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE81C;" FontSize="10" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                <TextBlock Text="From History" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="10"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
                                                CornerRadius="4" Padding="4,0"
                                                Visibility="{x:Bind IsReadOnlyFallback, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                                            <TextBlock Text="Read Only" Style="{StaticResource CaptionTextBlockStyle}" Foreground="White" FontSize="10"/>
                                        </Border>
                                        <Border Background="{ThemeResource SystemFillColorNeutralBrush}"
                                                CornerRadius="4" Padding="4,0"
                                                Visibility="{x:Bind TagsDisplay, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE1CB;" FontSize="10" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                <TextBlock Text="{x:Bind TagsDisplay, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="10" MaxWidth="200" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Border>
                                        <!-- Completion-actions indicator: otherwise the pipeline config is invisible outside the edit dialog -->
                                        <Border Background="{ThemeResource SystemFillColorNeutralBrush}"
                                                CornerRadius="4" Padding="4,0"
                                                Visibility="{x:Bind HasPipeline, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"
                                                ToolTipService.ToolTip="{x:Bind PipelineSummary, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE9D5;" FontSize="10" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                <TextBlock Text="Chained" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="10"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{x:Bind Path}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                                    <TextBlock Text="{x:Bind NextRunTime, Converter={StaticResource DateTimeConverter}}" 
                                               Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                                <ToggleSwitch Grid.Column="2" 
                                              IsOn="{x:Bind IsEnabled, Mode=OneWay}" 
                                              IsEnabled="{x:Bind IsReadOnlyFallback, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"
                                              PointerPressed="ToggleSwitch_PointerPressed"
                                              Toggled="ToggleSwitch_Toggled"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                </Grid> <!-- End TasksViewGrid -->

                <Frame x:Name="ContentFrame" Grid.Row="1" Visibility="Collapsed"/>
            </Grid>
            
        </NavigationView>

        <!-- Keyboard Shortcuts Dialog -->
        <ContentDialog x:Name="ShortcutsDialog"
                       Title="Keyboard Shortcuts"
                       CloseButtonText="Close"
                       DefaultButton="Close">
            <StackPanel Spacing="8" MinWidth="340">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                        <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                    </Grid.RowDefinitions>

                    <!-- Headers -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Shortcut" Style="{StaticResource CaptionTextBlockStyle}" FontWeight="SemiBold" Opacity="0.6" Margin="0,0,0,6"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Action"   Style="{StaticResource CaptionTextBlockStyle}" FontWeight="SemiBold" Opacity="0.6" Margin="0,0,0,6"/>

                    <!-- Rows -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl + N"  Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="New Task"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Ctrl + E"  Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Edit Selected Task"/>
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Ctrl + R"  Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="Run Selected Task"/>
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Delete"    Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="Delete Selected Task"/>
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="F5"        Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="Refresh Task List"/>
                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Esc"       Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" Text="Close Dialogs / Clear Selection"/>
                    <TextBlock Grid.Row="7" Grid.Column="0" Text="F1"        Style="{StaticResource BodyTextBlockStyle}" FontFamily="Consolas"/>
                    <TextBlock Grid.Row="7" Grid.Column="1" Text="Show This Dialog"/>
                </Grid>
            </StackPanel>
        </ContentDialog>

        <ContentDialog x:Name="TaskDetailsDialog"
                       CloseButtonText="Close"
                       DefaultButton="Close"
                       MinWidth="1000"
                       MaxWidth="1000">
            <ContentDialog.Resources>
                <x:Double x:Key="ContentDialogMaxWidth">2000</x:Double>
            </ContentDialog.Resources>
            <ScrollViewer x:Name="DetailsScrollViewer" MaxHeight="600" VerticalScrollBarVisibility="Visible" IsScrollInertiaEnabled="False">
                <StackPanel Spacing="12" Width="960">
                    <InfoBar IsOpen="{x:Bind ViewModel.SelectedTask.IsReadOnlyFallback, Mode=OneWay}" 
                             Severity="Warning" 
                             Title="Limited Access" 
                             Message="This task was discovered via event logs and cannot be fully managed without administrator privileges." 
                             IsClosable="False" 
                             Margin="0,0,0,8" />
                    <TextBlock x:Name="DialogTaskName" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock x:Name="DialogTaskDescription" TextWrapping="WrapWholeWords"/>
                    <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Author:" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,12,0"/>
                        <TextBlock x:Name="DialogTaskAuthor" Grid.Column="1"/>
                    </Grid>

                    <Grid Tapped="CategoryBadge_Tapped" ToolTipService.ToolTip="Filter by this category">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Category:" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <Border Grid.Column="1" Background="{ThemeResource SystemControlBackgroundAccentBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" MaxWidth="300">
                             <TextBlock x:Name="DialogTaskCategory" TextTrimming="CharacterEllipsis" Foreground="White" FontSize="12"/>
                        </Border>
                    </Grid>

                    <StackPanel x:Name="DialogTaskTagsPanel" Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Tags:" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,12,0"/>
                        <ItemsControl x:Name="DialogTaskTagsItems">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VariableSizedWrapGrid Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" 
                                            CornerRadius="4" Padding="6,2" Margin="0,0,4,4" 
                                            Tapped="TagBadge_Tapped" ToolTipService.ToolTip="Filter by this tag"
                                            MaxWidth="200">
                                        <TextBlock Text="{Binding}" Style="{StaticResource CaptionTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    
                    <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,8"/>
                    
                    <!-- History Section -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Recent History" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,0,16,0">
                            <ComboBox x:Name="HistoryFilterCombo" SelectedIndex="0" SelectionChanged="HistoryFilter_Changed" MinWidth="150">
                                <ComboBoxItem Content="Today" Tag="Today"/>
                                <ComboBoxItem Content="Yesterday" Tag="Yesterday"/>
                                <ComboBoxItem Content="This Week" Tag="Week"/>
                                <ComboBoxItem Content="All Time" Tag="All"/>
                            </ComboBox>
                            <Button x:Name="RefreshHistoryBtn" Click="RefreshHistory_Click" ToolTipService.ToolTip="Reload History">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <SymbolIcon Symbol="Refresh" />
                                    <TextBlock Text="Reload" />
                                </StackPanel>
                            </Button>
                            <Button x:Name="ExportHistoryCsvBtn" Content="Export CSV" Click="ExportHistoryCsv_Click" />
                            <Button x:Name="CopyHistoryBtn" Content="📋 Copy" Click="CopyHistory_Click"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Stats Panel -->
                    <Grid x:Name="HistoryStatsGrid" ColumnSpacing="12" Margin="0,0,0,8" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Grid Grid.Column="0" Padding="8,4" Background="Transparent" Tapped="StatTotal_Tapped" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="Total Runs" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                                <TextBlock x:Name="StatTotalRuns" Text="-" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                        
                        <Grid Grid.Column="1" Padding="8,4" Background="Transparent" Tapped="StatSuccess_Tapped" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="Success" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                                <TextBlock x:Name="StatSuccess" Text="-" FontWeight="SemiBold" Foreground="LightGreen"/>
                            </StackPanel>
                        </Grid>
                        
                        <Grid Grid.Column="2" Padding="8,4" Background="Transparent" Tapped="StatFailed_Tapped" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="Failed" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                                <TextBlock x:Name="StatFailed" Text="-" FontWeight="SemiBold" Foreground="LightSalmon"/>
                            </StackPanel>
                        </Grid>
                        
                        <Grid Grid.Column="3" Padding="8,4">
                            <StackPanel>
                                <TextBlock Text="Last Result" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                                <TextBlock x:Name="StatLastResult" Text="-" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                    
                    <ListView x:Name="InlineHistoryListView" MaxHeight="170" SelectionMode="Single" 
                              IsItemClickEnabled="True" ItemClick="HistoryList_ItemClick"
                              PreviewKeyDown="HistoryList_KeyDown" Margin="0,0,16,0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Time}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Result}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                        <Button x:Name="RunTaskButton" Content="Run Now" Click="RunTask_Click" Style="{StaticResource AccentButtonStyle}" IsEnabled="{x:Bind ViewModel.SelectedTask.IsReadOnlyFallback, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"/>
                        <Button x:Name="StopTaskButton" Content="Stop" Click="StopTask_Click" IsEnabled="{x:Bind ViewModel.SelectedTask.IsReadOnlyFallback, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"/>
                        <Button x:Name="EditTaskButton" Content="Edit" Click="EditTask_Click" IsEnabled="{x:Bind ViewModel.SelectedTask.IsReadOnlyFallback, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"/>
                        <Button x:Name="ExportTaskButton" Content="Export" Click="ExportTask_Click"/>
                        <Button x:Name="DeleteTaskButton" Content="Delete" Click="DeleteTask_Click" IsEnabled="{x:Bind ViewModel.SelectedTask.IsReadOnlyFallback, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </ContentDialog>

        <ContentDialog x:Name="TaskEditDialog"
                       PrimaryButtonText="Save"
                       CloseButtonText="Cancel"
                       DefaultButton="Primary"
                       PrimaryButtonClick="TaskEditDialog_PrimaryButtonClick">
            <ContentDialog.Resources>
                <x:Double x:Key="ContentDialogMaxWidth">1000</x:Double>
            </ContentDialog.Resources>
            <ScrollViewer x:Name="EditScrollViewer" MaxHeight="600" VerticalScrollBarVisibility="Auto" IsTabStop="False" Background="Transparent" PointerPressed="DialogScrollViewer_PointerPressed" IsScrollInertiaEnabled="False">
                <StackPanel Spacing="16" Width="750" Margin="0,8,12,8">
                <TextBlock x:Name="DlgTitleText" Text="Add or edit task" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,8,0,0"/>
                <InfoBar x:Name="EditTaskErrorBar" IsOpen="False" Severity="Error" Margin="0,0,0,8" />
                <TextBox x:Name="EditTaskName" PlaceholderText="Enter task name" Tag="ph">
                    <TextBox.Header>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock x:Name="DlgTaskNameLabel" Text="Task Name" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                      ToolTipService.ToolTip="The unique identifier for this task. It must be distinct within its folder in Windows Task Scheduler."/>
                        </StackPanel>
                    </TextBox.Header>
                </TextBox>
                <TextBox x:Name="EditTaskDescription" AcceptsReturn="True" Height="80">
                    <TextBox.Header>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock x:Name="DlgDescLabel" Text="Description" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                      ToolTipService.ToolTip="A human-readable note that explains what this task does. Visible in Task Scheduler and the details panel."/>
                        </StackPanel>
                    </TextBox.Header>
                </TextBox>
                <TextBox x:Name="EditTaskAuthor">
                    <TextBox.Header>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock x:Name="DlgAuthorLabel" Text="Author" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                      ToolTipService.ToolTip="The person or system that owns this task. Stored in the task XML; not enforced by Windows."/>
                        </StackPanel>
                    </TextBox.Header>
                </TextBox>
                
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <AutoSuggestBox x:Name="EditTaskCategory" PlaceholderText="e.g. Work" Tag="ph" 
                                   ItemsSource="{x:Bind ViewModel.SavedCategories}"
                                   QueryIcon="Find" 
                                   GotFocus="EditTaskCategory_GotFocus"
                                   Tapped="EditTaskCategory_GotFocus"
                                   TextChanged="EditTaskCategory_TextChanged"
                                   SuggestionChosen="EditTaskCategory_SuggestionChosen">
                        <AutoSuggestBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgCategoryLabel" Text="Category" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="Groups tasks together for filtering in the main list. Stored inside the task XML as the task's category label."/>
                            </StackPanel>
                        </AutoSuggestBox.Header>
                    </AutoSuggestBox>
                    <AutoSuggestBox x:Name="EditTaskTags" Grid.Column="1" PlaceholderText="e.g. urgent, sync (comma separated)" Tag="ph"
                                   QueryIcon="Find"
                                   GotFocus="EditTaskTags_GotFocus"
                                   Tapped="EditTaskTags_GotFocus"
                                   TextChanged="EditTaskTags_TextChanged"
                                   SuggestionChosen="EditTaskTags_SuggestionChosen">
                        <AutoSuggestBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgTagsLabel" Text="Tags" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="Custom comma-separated labels used for filtering inside this app. Tags are stored in the task Description field and are NOT a native Windows Task Scheduler feature."/>
                            </StackPanel>
                        </AutoSuggestBox.Header>
                    </AutoSuggestBox>
                </Grid>

                <ToggleSwitch x:Name="EditTaskEnabled" IsOn="True">
                    <ToggleSwitch.Header>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock x:Name="DlgEnabledLabel" Text="Enabled" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                      ToolTipService.ToolTip="When off, the task is disabled and will not run on any of its triggers, even if they fire."/>
                        </StackPanel>
                    </ToggleSwitch.Header>
                </ToggleSwitch>
                
                <!-- Completion Actions / Pipeline (v1.9) -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock x:Name="DlgPipelineLabel" Text="Completion Actions" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                      ToolTipService.ToolTip="Chain other tasks to this one. When this task finishes, its exit code decides whether the success or failure tasks are started. FluentTaskScheduler must be running for the chain to fire."/>
                        </StackPanel>
                        <TextBlock x:Name="DlgPipelineSummary" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" TextWrapping="Wrap"/>
                    </StackPanel>
                    <Button x:Name="ConfigurePipelineButton" Grid.Column="1" VerticalAlignment="Center" Click="ConfigurePipeline_Click" Content="Configure..."/>
                </Grid>

                <!-- Trigger Section -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <TextBlock x:Name="DlgTriggersTitle" Text="Triggers" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,8,0,0"/>
                
                <Grid ColumnSpacing="8" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ListView x:Name="TriggerList" Height="100" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" SelectionMode="Single" SelectionChanged="TriggerList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{Binding Descriptor}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <StackPanel Grid.Column="1" Spacing="4">
                        <Button x:Name="BtnAddTrigger" Content="+" Width="32" Click="BtnAddTrigger_Click" ToolTipService.ToolTip="Add Trigger"/>
                        <Button x:Name="BtnRemoveTrigger" Content="&#x2212;" Width="32" Click="BtnRemoveTrigger_Click" ToolTipService.ToolTip="Remove Trigger"/>
                        <Button x:Name="BtnMoveTriggerUp" Content="^" Width="32" Click="BtnMoveTriggerUp_Click" ToolTipService.ToolTip="Move Up" FontSize="12"/>
                        <Button x:Name="BtnMoveTriggerDown" Content="v" Width="32" Click="BtnMoveTriggerDown_Click" ToolTipService.ToolTip="Move Down" FontSize="11"/>
                    </StackPanel>
                </Grid>

                <StackPanel x:Name="TriggerDetailsPanel" Spacing="0" Visibility="Collapsed">
                <ComboBox x:Name="EditTaskTriggerType" SelectedIndex="0" SelectionChanged="EditTaskTriggerType_SelectionChanged" HorizontalAlignment="Stretch">
                    <ComboBox.Header>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock x:Name="DlgTriggerTypeLabel" Text="Trigger Type" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                      ToolTipService.ToolTip="Determines what event or schedule starts the task. &#x0a;&#x2022; Daily / Weekly / Monthly: time-based schedules.&#x0a;&#x2022; At Logon: runs when a user signs in.&#x0a;&#x2022; At Startup: runs when Windows boots.&#x0a;&#x2022; On an event: triggers from the Windows Event Log."/>
                        </StackPanel>
                    </ComboBox.Header>
                    <ComboBoxItem x:Name="DlgTriggerDaily" Content="Daily" Tag="Daily"/>
                    <ComboBoxItem x:Name="DlgTriggerWeekly" Content="Weekly" Tag="Weekly"/>
                    <ComboBoxItem x:Name="DlgTriggerMonthly" Content="Monthly" Tag="Monthly"/>
                    <ComboBoxItem x:Name="DlgTriggerLogon" Content="At Logon" Tag="AtLogon"/>
                    <ComboBoxItem x:Name="DlgTriggerStartup" Content="At Startup" Tag="AtStartup"/>
                    <ComboBoxItem x:Name="DlgTriggerOnce" Content="One Time" Tag="Once"/>
                    <ComboBoxItem x:Name="DlgTriggerEvent" Content="On an event" Tag="Event"/>
                    <ComboBoxItem x:Name="DlgTriggerSession" Content="On Workstation Lock/Unlock" Tag="SessionStateChange"/>
                </ComboBox>
                <StackPanel Spacing="8" Margin="0,8,0,0">
                     <StackPanel x:Name="PanelStartTime" Spacing="16">
                         <DatePicker x:Name="EditTaskStartDate" HorizontalAlignment="Left" SelectedDateChanged="EditTaskStartDate_SelectedDateChanged"/>
                         <TimePicker x:Name="EditTaskStartTime" HorizontalAlignment="Left" ClockIdentifier="24HourClock" TimeChanged="EditTaskStartTime_TimeChanged"/>
                     </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <CheckBox x:Name="EditTaskRandomDelay" Content="Delay task for up to (random delay):" Click="EditTaskRandomDelay_Click"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Adds a random wait (between 0 and the specified value) before the task actually starts. Useful to stagger tasks that trigger at the same time across many machines."/>
                        <TextBox x:Name="EditTaskRandomDelayVal" PlaceholderText="e.g. 1 hour" Width="120" Tag="ph"/>
                        <TextBlock x:Name="DlgDelayHint" Text="(e.g. 30s, 1m, 1h)" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                         <CheckBox x:Name="EditTaskStopAfter" Content="Stop task if runs longer than:" Click="EditTaskStopAfter_Click"/>
                         <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                   ToolTipService.ToolTip="Automatically terminates the task process if it has been running longer than the chosen duration. Helps prevent runaway processes."/>
                         <ComboBox x:Name="EditTaskStopAfterVal" IsEditable="True" Width="120">
                             <ComboBoxItem x:Name="DlgStop15m" Content="15 minutes" Tag="PT15M"/>
                             <ComboBoxItem x:Name="DlgStop30m" Content="30 minutes" Tag="PT30M"/>
                             <ComboBoxItem x:Name="DlgStop1h" Content="1 hour" Tag="PT1H"/>
                             <ComboBoxItem x:Name="DlgStop2h" Content="2 hours" Tag="PT2H"/>
                             <ComboBoxItem x:Name="DlgStop4h" Content="4 hours" Tag="PT4H"/>
                             <ComboBoxItem x:Name="DlgStop8h" Content="8 hours" Tag="PT8H"/>
                             <ComboBoxItem x:Name="DlgStop12h" Content="12 hours" Tag="PT12H"/>
                             <ComboBoxItem x:Name="DlgStop1d" Content="1 day" Tag="P1D"/>
                             <ComboBoxItem x:Name="DlgStop2d" Content="2 days" Tag="P2D"/>
                             <ComboBoxItem x:Name="DlgStop3d" Content="3 days" Tag="P3D"/>
                             <ComboBoxItem x:Name="DlgStop5d" Content="5 days" Tag="P5D"/>
                         </ComboBox>
                    </StackPanel>
                </StackPanel>

                <!-- Daily Trigger -->
                <StackPanel x:Name="PanelDaily" Visibility="Collapsed" Spacing="8" Margin="0,4,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <CheckBox x:Name="EditTaskDailyRecurrence" Content="Recur every" IsChecked="True" Checked="EditTaskDailyRecurrence_Checked" Unchecked="EditTaskDailyRecurrence_Checked" VerticalAlignment="Center"/>
                        <TextBox x:Name="DailyInterval" Width="80" Text="1" VerticalAlignment="Center" TextChanged="DailyInterval_TextChanged"/>
                        <TextBlock x:Name="DlgDaysSuffix" Text="day(s)" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <!-- Weekly Trigger -->
                <StackPanel x:Name="PanelWeekly" Visibility="Collapsed" Spacing="8" Margin="0,4,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="DlgWeeklyRecur" Text="Recur every" VerticalAlignment="Center"/>
                        <TextBox x:Name="WeeklyInterval" Text="1" InputScope="Number" Width="60" TextChanged="WeeklyInterval_TextChanged"/>
                        <TextBlock x:Name="DlgWeeksOn" Text="weeks on:" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <CheckBox x:Name="WeeklyMon" Content="Mon" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                        <CheckBox x:Name="WeeklyTue" Content="Tue" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                        <CheckBox x:Name="WeeklyWed" Content="Wed" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                        <CheckBox x:Name="WeeklyThu" Content="Thu" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                         <CheckBox x:Name="WeeklyFri" Content="Fri" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                        <CheckBox x:Name="WeeklySat" Content="Sat" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                        <CheckBox x:Name="WeeklySun" Content="Sun" Checked="WeeklyDay_CheckChanged" Unchecked="WeeklyDay_CheckChanged"/>
                    </StackPanel>
                </StackPanel>

                <!-- Monthly Trigger -->
                <StackPanel x:Name="PanelMonthly" Visibility="Collapsed" Spacing="12" Margin="0,4,0,0">
                    <TextBlock x:Name="DlgMonthsLabel" Text="Months:" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <Grid ColumnSpacing="8" RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <CheckBox x:Name="MonthJan" Content="Jan" Grid.Row="0" Grid.Column="0" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthFeb" Content="Feb" Grid.Row="0" Grid.Column="1" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthMar" Content="Mar" Grid.Row="0" Grid.Column="2" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthApr" Content="Apr" Grid.Row="0" Grid.Column="3" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthMay" Content="May" Grid.Row="0" Grid.Column="4" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthJun" Content="Jun" Grid.Row="0" Grid.Column="5" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>

                        <CheckBox x:Name="MonthJul" Content="Jul" Grid.Row="1" Grid.Column="0" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthAug" Content="Aug" Grid.Row="1" Grid.Column="1" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthSep" Content="Sep" Grid.Row="1" Grid.Column="2" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthOct" Content="Oct" Grid.Row="1" Grid.Column="3" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthNov" Content="Nov" Grid.Row="1" Grid.Column="4" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                        <CheckBox x:Name="MonthDec" Content="Dec" Grid.Row="1" Grid.Column="5" Checked="MonthlyMonth_CheckChanged" Unchecked="MonthlyMonth_CheckChanged"/>
                    </Grid>

                    <RadioButton x:Name="MonthlyRadioDays" Content="Days" IsChecked="True" GroupName="MonthlyMode" Tag="loc" Checked="MonthlyMode_Changed"/>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="24,0,0,0">
                         <TextBox x:Name="MonthlyDaysInput" PlaceholderText="e.g. 1, 15, Last" Width="200" Tag="ph" TextChanged="MonthlyDaysInput_TextChanged"/>
                         <TextBlock x:Name="DlgMonthlyDaysHint" Text="(comma separated, use 'Last' for last day)" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                    </StackPanel>

                    <RadioButton x:Name="MonthlyRadioOn" Content="On" GroupName="MonthlyMode" Tag="loc" Checked="MonthlyMode_Changed"/>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="24,0,0,0">
                        <ComboBox x:Name="MonthlyWeekCombo" SelectedIndex="0" SelectionChanged="MonthlyWeekOrDay_SelectionChanged">
                            <ComboBoxItem x:Name="DlgWeekFirst" Content="First"/>
                            <ComboBoxItem x:Name="DlgWeekSecond" Content="Second"/>
                            <ComboBoxItem x:Name="DlgWeekThird" Content="Third"/>
                            <ComboBoxItem x:Name="DlgWeekFourth" Content="Fourth"/>
                            <ComboBoxItem x:Name="DlgWeekLast" Content="Last"/>
                        </ComboBox>
                        <ComboBox x:Name="MonthlyDayCombo" SelectedIndex="0" SelectionChanged="MonthlyWeekOrDay_SelectionChanged">
                            <ComboBoxItem x:Name="DlgDayMon" Content="Monday"/>
                            <ComboBoxItem x:Name="DlgDayTue" Content="Tuesday"/>
                            <ComboBoxItem x:Name="DlgDayWed" Content="Wednesday"/>
                            <ComboBoxItem x:Name="DlgDayThu" Content="Thursday"/>
                            <ComboBoxItem x:Name="DlgDayFri" Content="Friday"/>
                            <ComboBoxItem x:Name="DlgDaySat" Content="Saturday"/>
                            <ComboBoxItem x:Name="DlgDaySun" Content="Sunday"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <!-- Event Log Trigger -->
                <StackPanel x:Name="PanelEvent" Visibility="Collapsed" Spacing="8" Margin="0,4,0,0">
                    <TextBox x:Name="EditTaskEventLog" Header="Log" PlaceholderText="Application, System, Security, etc." Text="Application" Tag="ph" TextChanged="EditTaskEventTrigger_TextChanged"/>
                    <TextBox x:Name="EditTaskEventSource" Header="Source" PlaceholderText="e.g., VSS, Outlook (Optional)" Tag="ph" TextChanged="EditTaskEventTrigger_TextChanged"/>
                    <TextBox x:Name="EditTaskEventId" Header="Event ID" PlaceholderText="e.g., 1000 (Optional)" InputScope="Number" Tag="ph" TextChanged="EditTaskEventTrigger_TextChanged"/>
                </StackPanel>

                <!-- Idle Trigger -->
                <StackPanel x:Name="PanelIdle" Visibility="Collapsed" Spacing="8" Margin="0,4,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="DlgIdleWait" Text="Wait for the computer to be idle for:" VerticalAlignment="Center"/>
                        <TextBox x:Name="EditTaskIdleDuration" PlaceholderText="e.g., 10m" Width="100" Text="PT10M" Tag="ph" TextChanged="EditTaskIdleDuration_TextChanged"/>
                        <TextBlock x:Name="DlgIdleHint" Text="(e.g. 5m, 10m, 30m)" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>

                <!-- Session State Change Trigger -->
                <StackPanel x:Name="PanelSessionState" Visibility="Collapsed" Spacing="8" Margin="0,4,0,0">
                    <ComboBox x:Name="EditTaskSessionStateType" Header="Trigger on" SelectedIndex="0" SelectionChanged="EditTaskSessionStateType_SelectionChanged" Tag="hdr">
                        <ComboBoxItem x:Name="DlgSessLock" Content="Workstation Lock" Tag="Lock"/>
                        <ComboBoxItem x:Name="DlgSessUnlock" Content="Workstation Unlock" Tag="Unlock"/>
                        <ComboBoxItem x:Name="DlgSessRdpOn" Content="Remote Desktop Connect" Tag="RemoteConnect"/>
                        <ComboBoxItem x:Name="DlgSessRdpOff" Content="Remote Desktop Disconnect" Tag="RemoteDisconnect"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Spacing="8" Margin="0,12,0,0">
                     <StackPanel Orientation="Horizontal" Spacing="4">
                         <CheckBox x:Name="EditTaskExpires" Content="Expire task on:" Click="EditTaskExpires_Click" Tag="loc"/>
                         <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                   ToolTipService.ToolTip="After this date and time the trigger will stop firing. The task itself remains; only the trigger is deactivated."/>
                     </StackPanel>
                     <StackPanel Spacing="8" Margin="28,0,0,0">
                         <DatePicker x:Name="EditTaskExpirationDate" HorizontalAlignment="Left"/>
                         <TimePicker x:Name="EditTaskExpirationTime" HorizontalAlignment="Left" ClockIdentifier="24HourClock"/>
                     </StackPanel>
                </StackPanel>
                </StackPanel> <!-- End TriggerDetailsPanel -->
                
                <!-- Action Section -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <TextBlock x:Name="DlgActionsTitle" Text="Actions" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,8,0,0"/>
                
                <Grid ColumnSpacing="8" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ListView x:Name="ActionList" Height="100" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" SelectionMode="Single" SelectionChanged="ActionList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock x:Name="DlgRunPrefix" Text="{Binding ElementName=PageRoot, Path=ViewModel.ActionRunPrefix}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Command}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <StackPanel Grid.Column="1" Spacing="4">
                        <Button x:Name="BtnAddAction" Content="+" Width="32" ToolTipService.ToolTip="Add Action">
                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem x:Name="DlgActionRunProg" Text="Run Program" Click="BtnAddAction_Click" />
                                    <MenuFlyoutItem x:Name="DlgActionEmail" Text="Send Email" Click="AddAction_SendEmail_Click" />
                                    <MenuFlyoutItem x:Name="DlgActionNotif" Text="Show Notification" Click="AddAction_ShowNotification_Click" />
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                        <Button x:Name="BtnRemoveAction" Content="-" Width="32" Click="BtnRemoveAction_Click" ToolTipService.ToolTip="Remove Action"/>
                        <Button x:Name="BtnMoveActionUp" Content="^" Width="32" Click="BtnMoveActionUp_Click" ToolTipService.ToolTip="Move Up" FontSize="12"/>
                        <Button x:Name="BtnMoveActionDown" Content="v" Width="32" Click="BtnMoveActionDown_Click" ToolTipService.ToolTip="Move Down" FontSize="11"/>
                    </StackPanel>
                </Grid>

                <StackPanel x:Name="ActionDetailsPanel" Spacing="0" Visibility="Collapsed">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="EditTaskActionCommand" PlaceholderText="e.g., notepad.exe or C:\Scripts\myscript.ps1" Grid.Column="0" TextChanged="EditTaskActionCommand_TextChanged" Tag="ph">
                            <TextBox.Header>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock x:Name="DlgProgramLabel" Text="Program / Script" VerticalAlignment="Center"/>
                                    <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                              ToolTipService.ToolTip="The full path or name of the executable, script, or document to launch. Use 'powershell.exe' with arguments for PowerShell scripts."/>
                                </StackPanel>
                            </TextBox.Header>
                        </TextBox>
                        <Button x:Name="BrowseActionButton" Content="Browse..." Click="BrowseAction_Click" VerticalAlignment="Bottom" Margin="0,0,0,6" Grid.Column="1" Tag="loc"/>
                    </Grid>
                    <TextBox x:Name="EditTaskArguments" PlaceholderText="e.g., /c echo hello or -File script.ps1" TextChanged="EditTaskArguments_TextChanged" Margin="0,8,0,0" Tag="ph">
                        <TextBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgArgsLabel" Text="Arguments (optional)" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="Command-line parameters passed directly to the program. For PowerShell: -ExecutionPolicy Bypass -File &quot;C:\path\to\script.ps1&quot;"/>
                            </StackPanel>
                        </TextBox.Header>
                    </TextBox>
                    <TextBox x:Name="EditTaskWorkingDirectory" PlaceholderText="e.g., C:\Scripts" TextChanged="EditTaskWorkingDirectory_TextChanged" Margin="0,8,0,0" Tag="ph">
                        <TextBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgWorkDirLabel" Text="Run in (optional)" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="Sets the working directory (current folder) for the process. Relative paths in the program or script will resolve relative to this folder."/>
                            </StackPanel>
                        </TextBox.Header>
                    </TextBox>
                    <TextBlock x:Name="DlgPsTip" Text="Tip: For PowerShell scripts, use 'powershell.exe' as Program and '-ExecutionPolicy Bypass -File &quot;C:\path\to\script.ps1&quot;' as Arguments" 
                               Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
                
                <!-- Repetition Section -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <TextBlock x:Name="DlgRepetitionTitle" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,8,0,4"/>
                <StackPanel Spacing="12">
                    <ComboBox x:Name="EditTaskRepetitionInterval" SelectedIndex="0" SelectionChanged="EditTaskRepetitionInterval_SelectionChanged">
                        <ComboBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgRepeatEveryLabel" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="After the task starts, re-trigger it at this interval until the 'For a duration of' limit is reached. Useful for tasks that must run repeatedly within a window."/>
                            </StackPanel>
                        </ComboBox.Header>
                        <ComboBoxItem x:Name="DlgRepeatNone" Tag=""/>
                        <ComboBoxItem x:Name="DlgRep5m" Content="5 minutes" Tag="PT5M"/>
                        <ComboBoxItem x:Name="DlgRep10m" Content="10 minutes" Tag="PT10M"/>
                        <ComboBoxItem x:Name="DlgRep15m" Content="15 minutes" Tag="PT15M"/>
                        <ComboBoxItem x:Name="DlgRep30m" Content="30 minutes" Tag="PT30M"/>
                        <ComboBoxItem x:Name="DlgRep1h" Content="1 hour" Tag="PT1H"/>
                        <ComboBoxItem x:Name="DlgRep2h" Content="2 hours" Tag="PT2H"/>
                        <ComboBoxItem x:Name="DlgRep4h" Content="4 hours" Tag="PT4H"/>
                        <ComboBoxItem x:Name="DlgRep6h" Content="6 hours" Tag="PT6H"/>
                        <ComboBoxItem x:Name="DlgRep12h" Content="12 hours" Tag="PT12H"/>
                    </ComboBox>
                    <ComboBox x:Name="EditTaskRepetitionDuration" SelectedIndex="0" SelectionChanged="EditTaskRepetitionDuration_SelectionChanged">
                        <ComboBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgDurationLabel" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="How long the repetition continues. 'Indefinitely' means the task keeps repeating until the trigger itself expires. A finite duration caps the total repetition window."/>
                            </StackPanel>
                        </ComboBox.Header>
                        <ComboBoxItem x:Name="DlgRepeatIndefinitely" Tag=""/>
                        <ComboBoxItem x:Name="DlgDur1h" Content="1 hour" Tag="PT1H"/>
                        <ComboBoxItem x:Name="DlgDur2h" Content="2 hours" Tag="PT2H"/>
                        <ComboBoxItem x:Name="DlgDur4h" Content="4 hours" Tag="PT4H"/>
                        <ComboBoxItem x:Name="DlgDur6h" Content="6 hours" Tag="PT6H"/>
                        <ComboBoxItem x:Name="DlgDur12h" Content="12 hours" Tag="PT12H"/>
                        <ComboBoxItem x:Name="DlgDur24h" Content="24 hours" Tag="PT24H"/>
                        <ComboBoxItem x:Name="DlgDur1d" Content="1 day" Tag="P1D"/>
                    </ComboBox>
                </StackPanel>
                
                <!-- Conditions Section -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <TextBlock x:Name="DlgConditionsTitle" Text="Conditions" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,12,0,4"/>
                <StackPanel Spacing="8">
                    <TextBlock x:Name="DlgStartOnlyIfLabel" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskOnlyIfIdle" Content="Computer is idle"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Only starts the task if the computer has had no user input for the specified idle period. Prevents the task from interrupting active work."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="24,0,0,0">
                        <TextBlock x:Name="DlgIdleForLabel" VerticalAlignment="Center"/>
                        <TextBox x:Name="EditTaskIdleDurationSetting" PlaceholderText="e.g., 10m" Width="100" Text="PT10M"/>
                        <CheckBox x:Name="EditTaskStopOnIdleEnd" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskOnlyIfAC" Content="Computer is on AC power"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Prevents the task from starting when the device is running on battery. Ideal for power-intensive operations."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4" Margin="24,0,0,0">
                        <CheckBox x:Name="EditTaskStopBatterySwitch"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="If the task is running and the power source switches from AC to battery, the task will be automatically stopped."/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskOnBattery"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Only starts the task when the device is running on battery (i.e., not plugged in to AC power)."/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskOnlyIfNetwork" Content="Network is available"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Only starts the task if a network connection is detected. Use the specific network dropdown below to restrict to a named connection."/>
                    </StackPanel>
                    <StackPanel Spacing="4" Margin="24,0,0,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Name="DlgSpecificNetworkLabel" VerticalAlignment="Center"/>
                            <ComboBox x:Name="EditTaskNetworkSelection" Width="250" MaxDropDownHeight="280">
                                <ComboBoxItem x:Name="DlgAnyNetworkItem" Tag="" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>
                        <InfoBar x:Name="NetworkAdminNotice" Severity="Informational" IsOpen="False" IsClosable="False"
                                 Title="Administrator required"
                                 Message="Specific network selection requires the app to run as administrator." Tag="loc"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskWakeToRun" Content="Wake the computer to run this task"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Instructs Windows to wake the computer from sleep or hibernation when it is time to run this task. Requires Wake Timers to be enabled in Power Options."/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Settings Section -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <TextBlock x:Name="DlgSettingsTitle" Text="Settings" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,12,0,4"/>
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskRunWithHighestPrivileges" Content="Run with highest privileges"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Requests UAC elevation so the task runs as a full administrator. Required for tasks that modify system files, the registry, or other protected resources."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskIsHidden" Content="Hidden task"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Hides this task from the default view in Windows Task Scheduler. The task still runs normally; it is just not shown to standard users browsing the scheduler."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskRunIfMissed" Content="Run task as soon as possible after a scheduled start is missed"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="If the computer was off or the task could not run at its scheduled time, Windows will run it immediately once the machine is available again."/>
                    </StackPanel>
                    
                    <ComboBox x:Name="EditTaskMultipleInstances">
                        <ComboBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgMultiInstanceLabel" Text="If the task is already running:" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="Controls behaviour when a new trigger fires while a previous instance is still running.&#x0a;&#x2022; Do not start: ignore the new trigger.&#x0a;&#x2022; Parallel: start another instance alongside.&#x0a;&#x2022; Queue: wait until the current one finishes.&#x0a;&#x2022; Stop existing: kill the current run and start fresh."/>
                            </StackPanel>
                        </ComboBox.Header>
                        <ComboBoxItem x:Name="DlgMultiIgnore" Content="Do not start a new instance" Tag="IgnoreNew" IsSelected="True"/>
                        <ComboBoxItem x:Name="DlgMultiParallel" Content="Run a new instance in parallel" Tag="Parallel"/>
                        <ComboBoxItem x:Name="DlgMultiQueue" Content="Queue a new instance" Tag="Queue"/>
                        <ComboBoxItem x:Name="DlgMultiStop" Content="Stop the existing instance" Tag="StopExisting"/>
                    </ComboBox>
                    
                    <ComboBox x:Name="EditTaskPriority" SelectedIndex="4">
                        <ComboBox.Header>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock x:Name="DlgPriorityLabel" Text="Task Priority:" VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                          ToolTipService.ToolTip="Sets the Windows thread scheduling priority for the task process. Higher priority tasks get more CPU time. 'Normal (7)' is recommended for most tasks; avoid Realtime unless absolutely necessary as it can starve other system processes."/>
                            </StackPanel>
                        </ComboBox.Header>
                        <ComboBoxItem x:Name="DlgPri0" Content="Realtime (0)" Tag="0"/>
                        <ComboBoxItem x:Name="DlgPri1" Content="High (1)" Tag="1"/>
                        <ComboBoxItem x:Name="DlgPri3" Content="Above Normal (3)" Tag="3"/>
                        <ComboBoxItem x:Name="DlgPri7" Content="Normal (7)" Tag="7"/>
                        <ComboBoxItem x:Name="DlgPri9" Content="Below Normal (9)" Tag="9"/>
                        <ComboBoxItem x:Name="DlgPri10" Content="Idle (10)" Tag="10"/>
                    </ComboBox>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskDeleteExpired" Content="Delete the task if it is not scheduled to run again"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Automatically removes the task from Task Scheduler once it has no future scheduled occurrences (e.g., a one-time task that has already run)."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskAllowHardTerminate" Content="Allow task to be forcefully terminated" IsChecked="True"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Allows Task Scheduler (or a user) to forcefully kill the task process when it is stopped. Disable this only for tasks that must always be allowed to finish gracefully."/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <CheckBox x:Name="EditTaskRestartOnFailure" Content="If the task fails, restart every:"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Automatically re-runs the task if it exits with a non-zero exit code (failure). Configure the interval and maximum number of attempts below."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="24,0,0,0">
                        <TextBox x:Name="EditTaskRestartInterval" PlaceholderText="e.g. 1 minute" Width="120" Tag="ph"/>
                        <TextBlock x:Name="DlgRestartHint" Text="(e.g. 30s, 1m, 5m)" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                        <TextBlock x:Name="DlgRestartUpTo" Text="Attempt to restart up to:" VerticalAlignment="Center"/>
                        <NumberBox x:Name="EditTaskRestartCount" Width="80" Value="3" Minimum="0" Maximum="999"/>
                        <TextBlock x:Name="DlgRestartTimes" Text="times" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- User Context Section -->
                <Rectangle Height="2" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,12,0,4"/>
                <TextBlock x:Name="DlgUserContextTitle" Text="User Context" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,12,0,4"/>
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <RadioButton x:Name="RunAsCurrentUser" Content="Run as current user" GroupName="UserContext" IsChecked="True" Checked="UserContextRadio_Checked"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Runs the task under the account of the currently logged-in user. The task can show windows and UI. The user must be logged in for the task to run."/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <RadioButton x:Name="RunAsSpecificUser" Content="Run as specific user" GroupName="UserContext" Checked="UserContextRadio_Checked"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Runs under a specific user account. Enter the username below. The task runs only when that user is logged in (unless 'Run with highest privileges' is also set)."/>
                    </StackPanel>
                    <TextBox x:Name="EditTaskRunAsUser" Header="Username" PlaceholderText="DOMAIN\Username or username@domain.com" Margin="24,0,0,0" IsEnabled="False" Tag="ph"/>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <RadioButton x:Name="RunAsSystem" Content="Run as SYSTEM (requires admin)" GroupName="UserContext" Click="RunAsSystem_Click" Checked="UserContextRadio_Checked"/>
                        <FontIcon Glyph="&#xE946;" FontSize="12" VerticalAlignment="Center" Opacity="0.6" Tapped="InfoIcon_Tapped"
                                  ToolTipService.ToolTip="Runs under the LocalSystem account with the highest possible privileges. The task runs whether or not any user is logged in and has no access to the interactive desktop. Requires this app to be running as administrator."/>
                    </StackPanel>
                    
                    <InfoBar x:Name="SystemUserWarning" Severity="Warning" IsOpen="False" IsClosable="False" Margin="0,4,0,0">
                        <InfoBar.Title>Administrator Privileges Required</InfoBar.Title>
                        <InfoBar.Message>
                            To create tasks that run as SYSTEM, this application must be running with administrator privileges. Right-click the app and select "Run as administrator".
                        </InfoBar.Message>
                    </InfoBar>
                </StackPanel>
            </StackPanel>
            </ScrollViewer>
        </ContentDialog>

        <!-- Global Snooze dialog (v1.9) -->
        <ContentDialog x:Name="SnoozeDialog"
                       DefaultButton="Primary"
                       PrimaryButtonClick="SnoozeDialog_PrimaryButtonClick">
            <StackPanel Spacing="14" MinWidth="420" Margin="0,8,0,0">
                <TextBlock x:Name="SnoozeDialogIntro" TextWrapping="Wrap" Opacity="0.8"/>

                <StackPanel Spacing="8">
                    <RadioButton x:Name="Snooze30m" GroupName="SnoozeDuration" IsChecked="True" Tag="30"/>
                    <RadioButton x:Name="Snooze1h" GroupName="SnoozeDuration" Tag="60"/>
                    <RadioButton x:Name="Snooze3h" GroupName="SnoozeDuration" Tag="180"/>
                    <RadioButton x:Name="SnoozeReboot" GroupName="SnoozeDuration" Tag="reboot"/>
                    <RadioButton x:Name="SnoozeCustom" GroupName="SnoozeDuration" Tag="custom" Checked="SnoozeCustom_Changed" Unchecked="SnoozeCustom_Changed"/>
                </StackPanel>

                <!-- IsEnabled is a Control property, so it is set on the pickers, not on the panel. -->
                <StackPanel Orientation="Horizontal" Spacing="12" Margin="28,0,0,0">
                    <DatePicker x:Name="SnoozeCustomDate" IsEnabled="False"/>
                    <TimePicker x:Name="SnoozeCustomTime" ClockIdentifier="24HourClock" IsEnabled="False"/>
                </StackPanel>

                <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>

                <CheckBox x:Name="SnoozeSuspendTriggers"/>
                <TextBlock x:Name="SnoozeSuspendHint" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" TextWrapping="Wrap" Margin="28,-8,0,0"/>

                <InfoBar x:Name="SnoozeDialogError" IsOpen="False" Severity="Error" IsClosable="True"/>
            </StackPanel>
        </ContentDialog>

        <ContentDialog x:Name="TaskHistoryDialog"
                       Title="Task History"
                       CloseButtonText="Close"
                       DefaultButton="Close">
            <ScrollViewer x:Name="HistoryScrollViewer" MaxHeight="600" VerticalScrollBarVisibility="Auto" IsScrollInertiaEnabled="False">
                <StackPanel Spacing="8" MinWidth="600">
                    <TextBlock x:Name="HistoryTaskInfo" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap"/>
                    <ListView x:Name="HistoryListView" MaxHeight="500">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Time" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Grid.Column="1" Text="Result" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Grid.Column="2" Text="Exit Code" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Grid.Column="3" Text="Message" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Time}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Result}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding ExitCode}"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Message}" TextWrapping="Wrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </ContentDialog>

        <!-- Custom Drag Visual for Elevated Mode -->
        <Canvas x:Name="CustomDragCanvas" IsHitTestVisible="False" Canvas.ZIndex="9999" Visibility="Collapsed">
            <Border x:Name="CustomDragVisual" 
                    Background="{ThemeResource LayerFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="12,8"
                    Opacity="0.8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon x:Name="CustomDragIcon" Glyph="&#xE8B7;" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock x:Name="CustomDragText" Text="Dragging item..." Style="{StaticResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Canvas>
    </Grid>
</Page>
