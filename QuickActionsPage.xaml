<Page
    x:Class="FluentTaskScheduler.QuickActionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:FluentTaskScheduler.ViewModels"
    xmlns:converters="using:FluentTaskScheduler.Converters"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:RunningToVisibilityConverter x:Key="RunningToVisibilityConverter"/>
        <converters:InverseStatusToBoolConverter x:Key="InverseStatusToBoolConverter"/>
    </Page.Resources>

    <Grid>
        <ScrollViewer x:Name="PageScrollViewer" Padding="24" IsScrollInertiaEnabled="False">
            <StackPanel Spacing="16">

                <!-- Header row -->
                <StackPanel Spacing="4">
                    <TextBlock x:Name="QuickActionsTitle" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock x:Name="QuickActionsDesc" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                </StackPanel>

                <!-- Action cards grid -->
                <GridView ItemsSource="{x:Bind ViewModel.Actions}" SelectionMode="None" HorizontalAlignment="Stretch">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:QuickActionItemViewModel">
                            <Grid Width="300" Height="180" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Margin="0,0,12,12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="{x:Bind Icon}" FontSize="18" Opacity="0.8" Foreground="{ThemeResource SystemControlBackgroundAccentBrush}"/>
                                        <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    
                                    <FontIcon Grid.Column="1" Glyph="&#xE73D;" FontSize="14" Opacity="0.6" Visibility="{x:Bind AdminRequired, Converter={StaticResource BoolToVisibilityConverter}}" ToolTipService.ToolTip="Requires Administrator privileges"/>
                                </Grid>
                                
                                <StackPanel Grid.Row="1" Margin="0,8,0,0">
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                                    
                                    <!-- Status Message -->
                                    <TextBlock Text="{x:Bind StatusMessage}" 
                                               Style="{StaticResource CaptionTextBlockStyle}" 
                                               Foreground="{ThemeResource SystemFillColorCriticalBrush}" 
                                               TextWrapping="Wrap" 
                                               Margin="0,4,0,0"
                                               MaxLines="2"
                                               TextTrimming="CharacterEllipsis"
                                               Visibility="{x:Bind StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>
                                
                                <Grid Grid.Row="2" Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Button Content="{x:Bind RunText}"
                                            HorizontalAlignment="Stretch" 
                                            Click="RunButton_Click" 
                                            Tag="{x:Bind}" 
                                            IsEnabled="{x:Bind Status, Converter={StaticResource InverseStatusToBoolConverter}, Mode=OneWay}"/>
                                    
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <ProgressRing IsActive="True" Width="16" Height="16" Visibility="{x:Bind Status, Converter={StaticResource RunningToVisibilityConverter}, Mode=OneWay}"/>
                                        <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" 
                                                   Style="{StaticResource CaptionTextBlockStyle}" 
                                                   Foreground="{x:Bind StatusColor, Mode=OneWay}"
                                                   FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
