<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
        <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
        <Version>1.9.0</Version>
        <AssemblyVersion>1.9.0</AssemblyVersion>
        <FileVersion>1.9.0</FileVersion>
        <OutputType>WinExe</OutputType>
        <UseWinUI>true</UseWinUI>
        <EnableMsixTooling>true</EnableMsixTooling>
        <WindowsPackageType>None</WindowsPackageType>
        <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
        <RuntimeIdentifier Condition="'$(Platform)' == 'ARM64'">win-arm64</RuntimeIdentifier>
        <RuntimeIdentifier Condition="'$(Platform)' != 'ARM64'">win-x64</RuntimeIdentifier>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <ImplicitUsings>enable</ImplicitUsings>
        <RootNamespace>FluentTaskScheduler</RootNamespace>
        <ApplicationManifest>app.manifest</ApplicationManifest>
        <Platforms>x64;ARM64</Platforms>
        <XamlCompilerSkipSavedState>true</XamlCompilerSkipSavedState>
        <ApplicationIcon>Assets\AppIcon.ico</ApplicationIcon>
        <DefineConstants>$(DefineConstants);DISABLE_XAML_GENERATED_MAIN</DefineConstants>
        <PublishReadyToRun>false</PublishReadyToRun>
        <!-- No PDBs at all (even in Debug) meant crash-log stack traces had no line numbers (3.11).
             Emit portable PDBs for local/Debug builds; still strip them for Release/publish. -->
        <DebugType Condition="'$(Configuration)' == 'Debug'">portable</DebugType>
        <DebugType Condition="'$(Configuration)' != 'Debug'">none</DebugType>
        <DebugSymbols Condition="'$(Configuration)' == 'Debug'">true</DebugSymbols>
        <DebugSymbols Condition="'$(Configuration)' != 'Debug'">false</DebugSymbols>
        <SelfContained>true</SelfContained>
    </PropertyGroup>

    <!-- FluentTaskScheduler.Tests lives in a subfolder of this project; without this exclude, the
         default **/*.cs glob below would also pick up its (and its obj/bin) source files. -->
    <ItemGroup>
        <Compile Remove="FluentTaskScheduler.Tests\**\*.cs" />
        <EmbeddedResource Remove="FluentTaskScheduler.Tests\**\*" />
        <None Remove="FluentTaskScheduler.Tests\**\*" />
        <Page Remove="FluentTaskScheduler.Tests\**\*" />
    </ItemGroup>

    <ItemGroup>
        <Content Include="Assets\SplashScreen.scale-200.png" />
        <Content Include="Assets\LockScreenLogo.scale-200.png" />
        <Content Include="Assets\Square150x150Logo.scale-200.png" />
        <Content Include="Assets\Square44x44Logo.scale-200.png" />
        <Content Include="Assets\Square44x44Logo.targetsize-24_altform-unplated.png" />
        <Content Include="Assets\StoreLogo.png" />
        <Content Include="Assets\Wide310x150Logo.scale-200.png" />
        <Content Include="Assets\MSI-Banner.bmp" />
        <Content Include="Assets\MSI-Logo.bmp" />
        <Content Include="Assets\AppIcon.ico">
            <CopyToOutputDirectory>Always</CopyToOutputDirectory>
        </Content>
        <Content Include="Assets\Scripts.json">
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
        </Content>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Toolkit.Uwp.Notifications" Version="7.1.3" />
        <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.5.240627000" />
        <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.22621.756" />
        <PackageReference Include="TaskScheduler" Version="2.12.2" />
        <PackageReference Include="Velopack" Version="0.0.1298" />
    </ItemGroup>
</Project>
