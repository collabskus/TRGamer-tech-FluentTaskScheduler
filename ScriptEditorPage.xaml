<Page
    x:Class="FluentTaskScheduler.ScriptEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <CommandBar Background="Transparent" DefaultLabelPosition="Right" HorizontalAlignment="Left">
            <AppBarButton x:Name="RunButton" Icon="Play" Click="RunButton_Click">
                <ToolTipService.ToolTip>
                    <ToolTip Content="Run Script (Ctrl+R)"/>
                </ToolTipService.ToolTip>
            </AppBarButton>
            <AppBarButton x:Name="StopButton" Icon="Stop" Click="StopButton_Click" IsEnabled="False"/>
            <AppBarSeparator/>
            <AppBarButton x:Name="SaveButton" Icon="Save" Click="SaveButton_Click"/>
            <AppBarSeparator/>
            <AppBarButton x:Name="ClearButton" Icon="Clear" Click="ClearButton_Click"/>
        </CommandBar>

        <!-- Editor Area -->
        <RichEditBox x:Name="CodeEditor"
                     Grid.Row="1"
                     Margin="0,12,0,0"
                     FontFamily="Consolas, 'Courier New', monospace"
                     FontSize="14"
                     AcceptsReturn="True"
                     IsSpellCheckEnabled="False"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch"
                     TextChanged="CodeEditor_TextChanged"
                     SelectionFlyout="{x:Null}"/>

        <!-- Console Divider -->
        <Rectangle Grid.Row="2" Height="1" HorizontalAlignment="Stretch" Fill="{ThemeResource SystemControlBackgroundAccentBrush}" Opacity="0.3" Margin="0,12"/>

        <!-- Output Console -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="OutputHeaderText" Text="Output Console" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,0,0,4"/>
            <ScrollViewer x:Name="OutputScrollViewer" Grid.Row="1" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" CornerRadius="4">
                <RichTextBlock x:Name="OutputConsole"
                           Margin="12"
                           FontFamily="Consolas, 'Courier New', monospace"
                           FontSize="12"
                           TextWrapping="Wrap"
                           IsTextSelectionEnabled="True">
                    <Paragraph x:Name="OutputParagraph"/>
                </RichTextBlock>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
